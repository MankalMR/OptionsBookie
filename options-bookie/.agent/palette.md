## 2025-03-07 - Options Roll Alert Box

**Learning:** The native `alert` modal used when an option is rolled successfully is jarring and disrupts the user flow, which goes against the smooth interaction philosophy of the app.
**Action:** Replace the native `alert` with a toast notification or inline success message using Shadcn components.
