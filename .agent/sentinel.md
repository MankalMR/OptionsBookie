## 2026-03-08 - Missing Authentication Check in API Route | **Learning:** All Next.js App Router API routes must manually check `getServerSession` at the beginning of the handler. Middleware is not reliable for auth redirects in this architecture. | **Prevention:** Ensure a strict linter rule or code review checklist step verifies the presence of `getServerSession` in every exported route handler outside of `/api/demo/`.
